<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- ✅ Permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- ✅ Queries - MUST be here, NOT inside <application> -->
    <queries>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="https" />
        </intent>
    </queries>

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TravelPractice"
        android:usesCleartextTraffic="true"
        tools:targetApi="31">

        <!-- 🤖 AI Chat Activity -->
        <activity
            android:name=".ChatActivity"
            android:exported="false"
            android:parentActivityName=".MainActivity"
            android:windowSoftInputMode="adjustResize"
            android:theme="@style/Theme.TravelPractice" />

        <!-- Admin Activities -->
        <activity
            android:name=".admin.AdminLoginActivity"
            android:label="Admin Login"
            android:exported="false" />

        <activity
            android:name=".admin.AdminPanelActivity"
            android:label="Admin Panel"
            android:exported="false" />

        <!-- Review Activities -->
        <activity
            android:name=".reviews.TownReviewsActivity"
            android:exported="false" />

        <activity
            android:name=".reviews.ReviewsActivity"
            android:exported="false"
            android:label="Reviews" />

        <activity
            android:name=".reviews.MapReviewsActivity"
            android:exported="false" />

        <!-- Trip Management Activities -->
        <activity
            android:name=".ItineraryActivity"
            android:exported="false" />

        <activity
            android:name=".ExpenseTrackerActivity"
            android:exported="false"
            android:parentActivityName=".MainActivity"
            android:theme="@style/Theme.TravelPractice" />

        <activity
            android:name=".ui.checklist.BagChecklistActivity"
            android:exported="false" />

        <activity
            android:name=".TripDetailActivity"
            android:exported="false" />

        <!-- Home Activity -->
        <activity
            android:name=".HomeActivity"
            android:exported="false" />

        <!-- Auth Activities -->
        <activity
            android:name=".RegisterActivity"
            android:exported="false" />

        <!-- Login Activity - App Entry Point -->
        <activity
            android:name=".LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Main Activity -->
        <activity
            android:name=".MainActivity"
            android:exported="false" />

        <!-- ✅ Google Maps API Key -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

    </application>

</manifest>